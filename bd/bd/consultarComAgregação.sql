use wm;

SELECT 
	REGIAO AS 'REGIÃO', 
	SUM(POPULACAO) AS TOTAL
FROM ESTADO
GROUP BY REGIAO;

SELECT 
	REGIAO AS 'REGIÃO', 
	SUM(POPULACAO) AS TOTAL
FROM ESTADO
GROUP BY REGIAO
ORDER BY TOTAL DESC;

SELECT 
	SUM(POPULACAO) AS TOTAL
FROM ESTADO;


SELECT 
	AVG(POPULACAO) AS TOTAL
FROM ESTADO;